# Typst

Container image for [typst](https://github.com/typst/typst)

## Verification

Images built from `main` carry a signed build provenance attestation:

```shell
gh attestation verify oci://ghcr.io/harryzcy/typst:latest \
  --repo harryzcy/container-images
```
